# SignaVision Testing

SignaVision uses automated tests to verify the frontend,
backend API, model contract, and image preprocessing pipeline.

## Frontend Tests

The frontend uses:

- Vitest
- Testing Library
- jsdom

Run:

```bash
cd client
npm run test:run